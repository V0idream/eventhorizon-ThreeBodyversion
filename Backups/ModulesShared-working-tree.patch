diff --git a/Gui/Scripts/ComponentList/ComponentTreeNode.cs b/Gui/Scripts/ComponentList/ComponentTreeNode.cs
index 5197d6b..4ce2b8d 100644
--- a/Gui/Scripts/ComponentList/ComponentTreeNode.cs
+++ b/Gui/Scripts/ComponentList/ComponentTreeNode.cs
@@ -10,19 +10,21 @@ namespace Gui.ComponentList
 {
     public class RootNode : IComponentTreeNode
     {
-        public RootNode(IComponentQuantityProvider quantityProvider, IDatabase database = null)
-        {
-            if (database != null)
-                _weaponNode = new WeaponNode(this, database.WeaponSlots);
-
-            _quantityProvider = quantityProvider;
-            _armorNode = CreateNode("$GroupArmor", new SpriteId("icons/icon_shield", SpriteId.Type.Default));
-            _energyNode = CreateNode("$GroupEnergy", new SpriteId("icons/icon_battery", SpriteId.Type.Default));
-            _droneNode = CreateNode("$GroupDrones", new SpriteId("icons/icon_drone", SpriteId.Type.Default));
-            _engineNode = CreateNode("$GroupEngines", new SpriteId("icons/icon_engine", SpriteId.Type.Default));
-            _specialNode = CreateNode("$GroupSpecial", new SpriteId("icons/icon_gear", SpriteId.Type.Default));
-
-            IsVisible = true;
+        public RootNode(IComponentQuantityProvider quantityProvider, IDatabase database = null)
+        {
+            _quantityProvider = quantityProvider;
+            _originalNode = new SourceNode(
+                "$ContentSourceOriginal",
+                new SpriteId("icons/icon_gear", SpriteId.Type.Default),
+                this,
+                database?.WeaponSlots);
+            _threeBodyNode = new SourceNode(
+                "$ContentSourceThreeBody",
+                new SpriteId("icons/icon_gear", SpriteId.Type.Default),
+                this,
+                database?.WeaponSlots);
+
+            IsVisible = true;
         }
 
         public void AddNode(IComponentTreeNode node, bool inTheEnd = false)
@@ -36,46 +38,25 @@ namespace Gui.ComponentList
         public IComponentTreeNode Parent { get { return null; } }
         public IComponentQuantityProvider QuantityProvider { get { return _quantityProvider; } }
 
-        public IComponentTreeNode Weapon { get { return _weaponNode; } }
-        public IComponentTreeNode Armor { get { return _armorNode; } }
-        public IComponentTreeNode Drone { get { return _droneNode; } }
-        public IComponentTreeNode Engine { get { return _engineNode; } }
-        public IComponentTreeNode Energy { get { return _energyNode; } }
-        public IComponentTreeNode Special { get { return _specialNode; } }
+        public IComponentTreeNode Weapon { get { return _originalNode.Weapon; } }
+        public IComponentTreeNode Armor { get { return _originalNode.Armor; } }
+        public IComponentTreeNode Drone { get { return _originalNode.Drone; } }
+        public IComponentTreeNode Engine { get { return _originalNode.Engine; } }
+        public IComponentTreeNode Energy { get { return _originalNode.Energy; } }
+        public IComponentTreeNode Special { get { return _originalNode.Special; } }
 
         public string Name { get { return "$GroupAll"; } }
         public SpriteId Icon { get { return new SpriteId("icons/icon_gear", SpriteId.Type.Default); } }
         public UnityEngine.Color Color { get { return CommonNode.DefaultColor; } }
         public bool IsVisible { get; set; }
 
-        public void Add(ComponentInfo componentInfo)
-        {
-            if (componentInfo.Data.Weapon != null)
-            {
-                _weaponNode.Add(componentInfo);
-                return;
-            }
-
-            switch (componentInfo.Data.DisplayCategory)
-            {
-                case ComponentCategory.Defense:
-                    _armorNode.Add(componentInfo);
-                    break;
-                case ComponentCategory.Energy:
-                    _energyNode.Add(componentInfo);
-                    break;
-                case ComponentCategory.Engine:
-                    _engineNode.Add(componentInfo);
-                    break;
-                case ComponentCategory.Drones:
-                    _droneNode.Add(componentInfo);
-                    break;
-                default:
-                    _specialNode.Add(componentInfo);
-                    break;
-            }
-
-            _count = -1;
+        public void Add(ComponentInfo componentInfo)
+        {
+            var sourceNode = componentInfo.Data.ContentSource == ContentSource.ThreeBody
+                ? _threeBodyNode
+                : _originalNode;
+            sourceNode.Add(componentInfo);
+            _count = -1;
         }
 
         public int ItemCount
@@ -108,12 +89,8 @@ namespace Gui.ComponentList
                 foreach (var node in _extraNodes1)
                     yield return node;
 
-                yield return _weaponNode;
-                yield return _armorNode;
-                yield return _energyNode;
-                yield return _droneNode;
-                yield return _engineNode;
-                yield return _specialNode;
+                yield return _originalNode;
+                yield return _threeBodyNode;
 
                 foreach (var node in _extraNodes2)
                     yield return node;
@@ -121,28 +98,110 @@ namespace Gui.ComponentList
             }
         }
 
-        private IComponentTreeNode CreateNode(string name, SpriteId icon)
-        {
-            return new CommonNode(name, icon, this);
-        }
-
-        private int _count = -1;
-        private readonly IComponentTreeNode _weaponNode;
-        private readonly IComponentTreeNode _armorNode;
-        private readonly IComponentTreeNode _energyNode;
-        private readonly IComponentTreeNode _droneNode;
-        private readonly IComponentTreeNode _engineNode;
-        private readonly IComponentTreeNode _specialNode;
+        private int _count = -1;
+        private readonly SourceNode _originalNode;
+        private readonly SourceNode _threeBodyNode;
         private readonly IComponentQuantityProvider _quantityProvider;
         private readonly List<IComponentTreeNode> _extraNodes1 = new List<IComponentTreeNode>();
-        private readonly List<IComponentTreeNode> _extraNodes2 = new List<IComponentTreeNode>();
-    }
+        private readonly List<IComponentTreeNode> _extraNodes2 = new List<IComponentTreeNode>();
+    }
+
+    public class SourceNode : IComponentTreeNode
+    {
+        public SourceNode(string name, SpriteId icon, IComponentTreeNode parent, WeaponSlots weaponSlots)
+        {
+            Name = name;
+            Icon = icon;
+            Parent = parent;
+
+            _weaponNode = weaponSlots != null
+                ? new WeaponNode(this, weaponSlots, true)
+                : new CommonNode("$GroupWeapon", new SpriteId("textures/icons/icon_weapon", SpriteId.Type.Default), this, true);
+            _armorNode = CreateNode("$GroupArmor", new SpriteId("icons/icon_shield", SpriteId.Type.Default));
+            _energyNode = CreateNode("$GroupEnergy", new SpriteId("icons/icon_battery", SpriteId.Type.Default));
+            _droneNode = CreateNode("$GroupDrones", new SpriteId("icons/icon_drone", SpriteId.Type.Default));
+            _engineNode = CreateNode("$GroupEngines", new SpriteId("icons/icon_engine", SpriteId.Type.Default));
+            _specialNode = CreateNode("$GroupSpecial", new SpriteId("icons/icon_gear", SpriteId.Type.Default));
+        }
+
+        public IComponentTreeNode Parent { get; }
+        public IComponentQuantityProvider QuantityProvider => Parent.QuantityProvider;
+        public string Name { get; }
+        public SpriteId Icon { get; }
+        public UnityEngine.Color Color => CommonNode.DefaultColor;
+        public bool IsVisible => true;
+
+        public IComponentTreeNode Weapon => _weaponNode;
+        public IComponentTreeNode Armor => _armorNode;
+        public IComponentTreeNode Drone => _droneNode;
+        public IComponentTreeNode Engine => _engineNode;
+        public IComponentTreeNode Energy => _energyNode;
+        public IComponentTreeNode Special => _specialNode;
+
+        public void Add(ComponentInfo componentInfo)
+        {
+            if (componentInfo.Data.Weapon != null)
+                _weaponNode.Add(componentInfo);
+            else
+            {
+                switch (componentInfo.Data.DisplayCategory)
+                {
+                    case ComponentCategory.Defense: _armorNode.Add(componentInfo); break;
+                    case ComponentCategory.Energy: _energyNode.Add(componentInfo); break;
+                    case ComponentCategory.Engine: _engineNode.Add(componentInfo); break;
+                    case ComponentCategory.Drones: _droneNode.Add(componentInfo); break;
+                    default: _specialNode.Add(componentInfo); break;
+                }
+            }
+
+            _count = -1;
+        }
+
+        public int ItemCount
+        {
+            get
+            {
+                if (_count < 0)
+                    _count = Children.GetItemCount();
+                return _count;
+            }
+        }
+
+        public IEnumerable<IComponentTreeNode> Nodes => Children.ChildrenNodes();
+        public IEnumerable<ComponentInfo> Components => Children.ChildrenComponents();
+        public void Clear() => Children.Clear();
+
+        private IEnumerable<IComponentTreeNode> Children
+        {
+            get
+            {
+                yield return _weaponNode;
+                yield return _armorNode;
+                yield return _energyNode;
+                yield return _droneNode;
+                yield return _engineNode;
+                yield return _specialNode;
+            }
+        }
+
+        private IComponentTreeNode CreateNode(string name, SpriteId icon) =>
+            new CommonNode(name, icon, this, true);
+
+        private int _count = -1;
+        private readonly IComponentTreeNode _weaponNode;
+        private readonly IComponentTreeNode _armorNode;
+        private readonly IComponentTreeNode _energyNode;
+        private readonly IComponentTreeNode _droneNode;
+        private readonly IComponentTreeNode _engineNode;
+        private readonly IComponentTreeNode _specialNode;
+    }
 
     public class WeaponNode : IComponentTreeNode
     {
-        public WeaponNode(IComponentTreeNode parent, WeaponSlots weaponSlots)
-        {
-            _parent = parent;
+        public WeaponNode(IComponentTreeNode parent, WeaponSlots weaponSlots, bool keepVisible = false)
+        {
+            _parent = parent;
+            _keepVisible = keepVisible;
 
             foreach (var slot in weaponSlots.Slots)
                 if (_groupMap.TryAdd(slot.Letter, _groups.Count))
@@ -186,7 +245,7 @@ namespace Gui.ComponentList
                 if (_count < 0)
                     _count = Children.GetItemCount();
 
-                return _count;
+                return _keepVisible ? System.Math.Max(3, _count) : _count;
             }
         }
 
@@ -202,7 +261,8 @@ namespace Gui.ComponentList
             return new CommonNode(name, icon, this);
         }
 
-        private int _count = -1;
+        private int _count = -1;
+        private readonly bool _keepVisible;
         private readonly IComponentTreeNode _parent;
         private readonly Dictionary<char,int> _groupMap = new();
         private readonly List<IComponentTreeNode> _groups = new();
@@ -251,11 +311,12 @@ namespace Gui.ComponentList
 
     public class CommonNode : IComponentTreeNode
     {
-        public CommonNode(string name, SpriteId icon, IComponentTreeNode parent)
-        {
-            _parent = parent;
-            _name = name;
-            _icon = icon;
+        public CommonNode(string name, SpriteId icon, IComponentTreeNode parent, bool keepVisible = false)
+        {
+            _parent = parent;
+            _name = name;
+            _icon = icon;
+            _keepVisible = keepVisible;
         }
 
         public IComponentTreeNode Parent { get { return _parent; } }
@@ -285,9 +346,9 @@ namespace Gui.ComponentList
             get
             {
                 if (_count < 0)
-                    _count = _components.Values.GetItemCount();
-
-                return _count;
+                    _count = _components.Values.GetItemCount();
+
+                return _keepVisible ? System.Math.Max(3, _count) : _count;
             }
         }
 
@@ -300,8 +361,9 @@ namespace Gui.ComponentList
         }
 
         private int _count;
-        private readonly string _name;
-        private readonly SpriteId _icon;
+        private readonly string _name;
+        private readonly SpriteId _icon;
+        private readonly bool _keepVisible;
         private readonly IComponentTreeNode _parent;
         private readonly Dictionary<int, IComponentTreeNode> _components = new Dictionary<int, IComponentTreeNode>();
 
diff --git a/Gui/Scripts/DebugConsole/DebugConsole.cs b/Gui/Scripts/DebugConsole/DebugConsole.cs
index d6c1ba9..e39ff4a 100644
--- a/Gui/Scripts/DebugConsole/DebugConsole.cs
+++ b/Gui/Scripts/DebugConsole/DebugConsole.cs
@@ -12,8 +12,13 @@ namespace Gui.DebugConsole
         [SerializeField] private int _maxMessageCount = 100;
 
         private readonly List<LogEntry> _messages = new();
-        private IDebugConsoleLogger _debugLogger;
-
+        private IDebugConsoleLogger _debugLogger;
+
+        private void Awake()
+        {
+            _button.SetActive(false);
+        }
+
         [Inject]
         private void Initialize(IDebugConsoleLogger debugLogger)
         {
@@ -26,11 +31,10 @@ namespace Gui.DebugConsole
             _debugLogger.MessageReceived -= OnDebugMessageReceived;
         }
 
-        public void OnDebugMessageReceived()
-        {
-            if (!this) return;
-            _button.SetActive(true);
-        }
+        public void OnDebugMessageReceived()
+        {
+            // Keep collecting diagnostics, but do not show the intrusive red error button.
+        }
 
         private void OnEnable()
         {
diff --git a/Localization/Resources/Localization/Chinese/Quests.xml b/Localization/Resources/Localization/Chinese/Quests.xml
index 49e28a1..9fbcff4 100644
--- a/Localization/Resources/Localization/Chinese/Quests.xml
+++ b/Localization/Resources/Localization/Chinese/Quests.xml
@@ -104,4 +104,7 @@
     <string name="Covidr_Message1">请求紧急支援！我们面临着前所未见的敌人，可能撑不了太久了…</string>
     <string name="Covidr_Message2">我们正受到攻击。</string>
     <string name="Covidr_Message3">感激涕零，指挥官，我们的国家会永远记住这一切的。</string>
+    <string name="Quest_ThreeBodyStarterGift">三体模组：开局补给</string>
+    <string name="ThreeBodyStarterGift_Message">检测到新的宇宙规则。接取此任务后将立即获得全部可用飞船、全部部件与卫星各 99 个、无限技能点，以及大量货币和所有势力的研发点。</string>
+    <string name="ThreeBodyStarterGift_Accept">接取并领取全部补给</string>
 </resources>
diff --git a/Localization/Resources/Localization/English/Quests.xml b/Localization/Resources/Localization/English/Quests.xml
index 1cfae38..3c88ce1 100644
--- a/Localization/Resources/Localization/English/Quests.xml
+++ b/Localization/Resources/Localization/English/Quests.xml
@@ -113,4 +113,7 @@
     <string name="Covidr_Message2">We are under attack.</string>
     <string name="Covidr_Message3">We are very grateful, Commander. Our nation will never forget that.</string>
 
+    <string name="Quest_ThreeBodyStarterGift">Three-Body Mod: Starter Supply</string>
+    <string name="ThreeBodyStarterGift_Message">Accept this mission to immediately receive every available ship, 99 of every component and satellite, unlimited skill points, abundant currencies, and research points for every faction.</string>
+    <string name="ThreeBodyStarterGift_Accept">Accept and claim all supplies</string>
 </resources>
diff --git a/ShipEditor/Scripts/UI/ComponentItem.cs b/ShipEditor/Scripts/UI/ComponentItem.cs
index d61011a..8873219 100644
--- a/ShipEditor/Scripts/UI/ComponentItem.cs
+++ b/ShipEditor/Scripts/UI/ComponentItem.cs
@@ -235,8 +235,13 @@ namespace ShipEditor.UI
 
             if (data.Damage.Corrosive > 0)
                 yield return new KeyValuePair<string, string>(settings.CorrosiveDamageText, data.Damage.Corrosive.ToString(_floatFormat) + damageSuffix);
-            else if (data.Dps.Corrosive > 0)
-                yield return new KeyValuePair<string, string>(settings.CorrosiveDpsText, data.Dps.Corrosive.ToString(_floatFormat) + damageSuffix);
+            else if (data.Dps.Corrosive > 0)
+                yield return new KeyValuePair<string, string>(settings.CorrosiveDpsText, data.Dps.Corrosive.ToString(_floatFormat) + damageSuffix);
+
+            if (data.Damage.True > 0)
+                yield return new KeyValuePair<string, string>("$TrueDamage", data.Damage.True.ToString(_floatFormat) + damageSuffix);
+            else if (data.Dps.True > 0)
+                yield return new KeyValuePair<string, string>("$TrueDPS", data.Dps.True.ToString(_floatFormat) + damageSuffix);
 
             if (data.Damage.Repair > 0)
                 yield return new KeyValuePair<string, string>("$WeaponRepair", data.Damage.Repair.ToString(_floatFormat) + damageSuffix);
